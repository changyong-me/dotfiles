interface:
  display_name: "Resolve Issues"
  short_description: "Resolve agent-ready issues and open or update a PR"
  default_prompt: "Use $resolve-issues to resolve agent-ready issues and open or update a PR."

policy:
  allow_implicit_invocation: false
