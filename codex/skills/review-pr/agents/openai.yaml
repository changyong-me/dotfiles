interface:
  display_name: "Review PR"
  short_description: "Review a PR against standards and spec"
  default_prompt: "Use $review-pr to review this pull request against standards and spec."

policy:
  allow_implicit_invocation: false
