interface:
  display_name: "Create Issues"
  short_description: "Draft vertical-slice issues and create them after approval"
  default_prompt: "Use $create-issues to draft vertical-slice issues and create them after approval."

policy:
  allow_implicit_invocation: false
